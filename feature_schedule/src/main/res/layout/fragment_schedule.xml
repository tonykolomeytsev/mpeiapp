<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <TextView
            style="@style/H2"
            android:id="@+id/header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginTop="12sp"
            app:layout_constraintTop_toTopOf="parent"
            android:text="Вторник, 7 мая"/>

    <TextView
            style="@style/H6.C8"
            android:id="@+id/description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            app:layout_constraintTop_toBottomOf="@id/header"
            android:text="Сейчас идет четная неделя №12"/>

    <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerDayItems"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="24dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:clipToPadding="false"
            app:layout_constraintTop_toBottomOf="@id/description"
            tools:listitem="@layout/item_week_days"/>

    <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPagerClasses"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="8dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/recyclerDayItems" />

    <kekmech.ru.coreui.banner.BannerContainer
            app:layout_constraintTop_toTopOf="parent"
            android:id="@+id/bannerContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>